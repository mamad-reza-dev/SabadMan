import * as CartViews from './cart';
import * as ProductViews from './products';
import { notificationView } from './common/notificationView';

export const Views = {
  Cart: CartViews,
  Product: ProductViews,
  Notification: notificationView,
};
